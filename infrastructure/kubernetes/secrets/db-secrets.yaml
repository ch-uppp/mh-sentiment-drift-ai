kubectl create secret generic db-secrets \
  --from-literal=POSTGRES_DB=sentiment_drift \
  --from-literal=POSTGRES_USER=postgres \
  --from-literal=POSTGRES_PASSWORD=your-secure-password \
  --from-literal=DATABASE_URL=postgresql://postgres:your-secure-password@postgresql-service:5432/sentiment_drift \
  -n sentiment-drift
