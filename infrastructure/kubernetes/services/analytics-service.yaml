apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: sentiment-drift
  labels:
    app: analytics
    component: microservice
spec:
  selector:
    app: analytics
  ports:
  - name: http
    port: 8005
    targetPort: 8005
    protocol: TCP
  type: ClusterIP
