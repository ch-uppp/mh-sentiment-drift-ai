apiVersion: v1
kind: Service
metadata:
  name: drift-detection-service
  namespace: sentiment-drift
  labels:
    app: drift-detection
    component: microservice
spec:
  selector:
    app: drift-detection
  ports:
  - name: http
    port: 8002
    targetPort: 8002
    protocol: TCP
  type: ClusterIP
